# MuscleMap Frontend

This frontend now uses:

- Plain HTML pages
- Plain JavaScript files
- Custom CSS
- Bootstrap via CDN for layout and base components

There is no React, Vite, Tailwind, or icon/font dependency required by the frontend pages.

Open `frontend/index.html` directly in a browser or serve the `frontend/` directory with any static file server.
